/**
 * The detect modes the graphz support.
 * @static
 * @constant
 * @name DETECT_MODES
 * @type {{AREA: number, POINT: number}}
 */
export const DETECT_MODES = {
    POINT:  0,
    AREA:   1,
};

/**
 * The icon type of node.
 * @static
 * @constant
 * @name ICON_TYPE
 * @type {{COLOR: number, IMAGE: number, POINT: number}}
 */
export const ICON_TYPE = {
    COLOR: 0,
    IMAGE: 1,
    FONT: 2,
};